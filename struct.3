#include <stdio.h>
#include <stdlib.h>
int i;
int k;
int j;
struct aluno{
        char nome[20];
        char sobre[20];
        int idade;
        float notas[4];
    };
void coleta_aluno(struct aluno* tu){
    struct aluno eu[4];
        for(k=0;k<4;k++){
            printf("NOME:\n");
            scanf("%s",tu->nome);
            printf("SOBRENOME:\n");
            scanf("%s",tu->sobre);
            printf("IDADE:\n");
            scanf("%d",&tu->idade);
            printf("NOTAS:\n");
            for(j=0;j<4;j++){
                scanf("%f",&tu->notas[j]);
            }
        }
    return 0;
    }

void mostra_aluno(struct aluno eu){
    printf("\nNome e Sobrenome: %s %s",eu.nome,eu.sobre);
    printf("\nIdade:%d",eu.idade);
    for(i=0;i<4;i++){
        printf("\n%dº Nota:%g",i+1,eu.notas[i]);
    }
    return 0;
}
int main()
{
    for(k=0;k<4;k++) coleta_aluno(&eu[k]);
    for(k=0;k<4;k++) mostra_aluno(eu[k]);
    return 0;
}
