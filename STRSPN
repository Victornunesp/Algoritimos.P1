#include <stdio.h>
#include <string.h>

int main ()
{
   int len;
   const char str1[] = "Z";
   const char str2[] = "5";

   len = strcspn(str1, str2);

   printf("Primeiro caracter encontrado na posicao %d\n", len + 1);

   return(0);
}
