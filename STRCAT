#include <stdio.h>
#include <string.h>
#include <locale.h>

int main ()
{
   setlocale(LC_ALL,"portuguese");
   char src[50], dest[50];

   strcpy(src,  "Aqui termina");
   strcpy(dest, "Aqui começa e ");

   strcat(dest, src);

   printf("Valor final de dest : |%s|", dest);

   return(0);
}
